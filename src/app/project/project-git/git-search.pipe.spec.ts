import { GitSearchPipe } from './git-search.pipe';

describe('GitSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GitSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
